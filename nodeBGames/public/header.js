function generateHeader() {
    var header = document.getElementById("title_section")
    var header_img = document.createElement("img")
    header_img.setAttribute("src", "rose.png")
    header_img.setAttribute("alt", "RHIT Logo")
    header_img.setAttribute("width", "128")
    header_img.setAttribute("height", "128")
    header_img.setAttribute("id", "img_logo")
    var text = document.createTextNode("Rose-Hulman Board Game Club Rental Service")
    header.appendChild(header_img)
    header.appendChild(text)

    var menu = document.getElementById("top_menu")
    var item = document.createAttribute("li")
    var link = document.createAttribute("a")
    link.setAttribute("type", "button")
    link.setAttribute("class", "menu_item")
    link.setAttribute("href", "javascript:goToHomepage()")
    text = document.createTextNode("Home")
    link.appendChild(text)
    item.appendChild(link)
    menu.appendChild(item)

    item = document.createAttribute("li")
    link = document.createAttribute("a")
    link.setAttribute("type", "button")
    link.setAttribute("class", "menu_item")
    link.setAttribute("href", "javascript:goToGamepage()")
    text = document.createTextNode("Game")
    link.appendChild(text)
    item.appendChild(link)
    menu.appendChild(item)

    item = document.createAttribute("li")
    link = document.createAttribute("a")
    link.setAttribute("type", "button")
    link.setAttribute("class", "menu_item")
    link.setAttribute("href", "javascript:goToClubpage()")
    text = document.createTextNode("Club")
    link.appendChild(text)
    item.appendChild(link)
    menu.appendChild(item)

    item = document.createAttribute("li")
    link = document.createAttribute("a")
    link.setAttribute("type", "button")
    link.setAttribute("class", "menu_item")
    link.setAttribute("href", "javascript:goToHelp()")
    text = document.createTextNode("Help")
    link.appendChild(text)
    item.appendChild(link)
    menu.appendChild(item)

    item = document.createAttribute("li")
    link = document.createAttribute("a")
    link.setAttribute("type", "button")
    link.setAttribute("class", "menu_item")
    link.setAttribute("href", "javascript:goToProfile()")
    text = document.createTextNode("Profile")
    link.appendChild(text)
    item.appendChild(link)
    menu.appendChild(item)

    item = document.createAttribute("li")
    link = document.createAttribute("a")
    link.setAttribute("type", "button")
    link.setAttribute("class", "menu_item")
    link.setAttribute("href", "javascript:goToCheckOut()")
    text = document.createTextNode("Check Out")
    link.appendChild(text)
    item.appendChild(link)
    menu.appendChild(item)
}